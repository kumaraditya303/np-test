[build-system]
build-backend = "mesonpy"
requires = ["meson-python>=0.15", "numpy>=2.0"]

[project]
name = "datetime-metadata"
version = "0.1.0"
description = "Limited C API + NumPy datetime64 unit-metadata demo/test"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["numpy>=2.0"]
